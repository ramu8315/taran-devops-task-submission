apiVersion: v1
kind: Service
metadata:
  name: {{ include "score-api.fullname" . }}
  labels:
    {{- include "score-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "score-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
